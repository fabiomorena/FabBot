# ============================================================
# FabBot – Umgebungsvariablen
# Kopiere diese Datei: cp .env.example .env
# ============================================================

# ── Pflichtfelder ────────────────────────────────────────────

# Anthropic API Key (claude.ai → API Keys)
ANTHROPIC_API_KEY=sk-ant-...

# Telegram Bot Token (via @BotFather auf Telegram)
TELEGRAM_BOT_TOKEN=...

# Deine Telegram User-ID (via @userinfobot auf Telegram)
# Mehrere IDs mit Komma trennen: 123456789,987654321
TELEGRAM_ALLOWED_USER_IDS=123456789

# ── Optionale Felder (Web-Suche) ─────────────────────────────

# Tavily API Key – https://tavily.com
# Wird für erweiterte Web-Suche verwendet (empfohlen)
TAVILY_API_KEY=tvly-...

# Brave Search API Key – https://brave.com/search/api/
# Wird als Fallback für Web-Suche verwendet
BRAVE_API_KEY=BSA...

# ── Optionale Felder (TTS) ───────────────────────────────────

# ElevenLabs API Key – https://elevenlabs.io
# Primärer TTS-Provider (natürlichere Stimmen als edge-tts)
# Ohne Key: automatischer Fallback auf edge-tts
ELEVENLABS_API_KEY=...

# ElevenLabs Voice ID – Voice Library → Stimme auswählen → ID aus URL
# Default: Ami
ELEVENLABS_VOICE_ID=1YdlDDaYSxqFmFZDaZ1z
# ElevenLabs Voice Settings (optional, Werte zwischen 0.0 und 1.0)
# ELEVENLABS_STABILITY=0.5
# ELEVENLABS_SIMILARITY_BOOST=0.75

# TTS aktivieren/deaktivieren (default: true)
# TTS_ENABLED=true

# ── Optionale Felder (Sonstiges) ─────────────────────────────

# Optionale externe Pfade fuer file_agent (colon-separated)
# FABBOT_EXTRA_PATHS=/Volumes/MyDrive/foo:/Volumes/MyDrive/bar

# Context Window fuer chat_agent (Anzahl Messages die an den LLM übergeben werden)
# Default: 40 (~20 Gesprächsrunden). SQLite bleibt vollständig.
# CHAT_CONTEXT_WINDOW=40

# Party Report – jeden Mittwoch 20:00 Uhr (0=Mo, 1=Di, 2=Mi, ...)
# PARTY_REPORT_TIME=20:00
# PARTY_REPORT_DAY=2
