[tool.mypy]
ignore_missing_imports = true
python_version = "3.12"

[tool.ruff]
line-length = 120

[tool.ruff.lint]
ignore = [
    "E402",  # module-level import not at top – intentional in several modules (lazy imports, circular import avoidance)
]
